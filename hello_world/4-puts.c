#include <stdio.h>
#include <string.h>

/**
 * main - This C program use puts to print
 * Return: devuelve 0
 */
int main(void)
{
	char res[] = "\"Programming is like building a multilingual puzzle";


	puts(res);

	(return 0);
}
